#pragma once

//========================================================================
// This conversion was produced by the Free Edition of
// Java to C++ Converter courtesy of Tangible Software Solutions.
// Order the Premium Edition at https://www.tangiblesoftwaresolutions.com
//========================================================================

#include "a.h"
#include "c.h"
#include <vector>

//JAVA TO C++ CONVERTER NOTE: Forward class declarations:
namespace android { namespace support { namespace v4 { namespace g { template<typename Ktypename V>class a; } } } }

namespace android
{
	namespace support
	{
		namespace v4
		{
			namespace g
			{

				class b
				{
			  public:
//JAVA TO C++ CONVERTER NOTE: Fields cannot have the same name as methods:
				  static std::vector<int> const a_Renamed = std::vector<int>(0);

//JAVA TO C++ CONVERTER NOTE: Members cannot have the same name as their enclosing type:
				  static std::vector<long long> const b_Renamed = std::vector<long long>(0);

				  static std::vector<void*> const c;

				  static int a(std::vector<int> &paramArrayOfInt, int paramInt1, int paramInt2);

				  static bool a(void *paramObject1, void *paramObject2);
				};

			}
		}
	}
}
